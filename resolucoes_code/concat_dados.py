# Vamos receber dois dados diferentes do usuário e concatena-los em uma única string?!

resultado = f"{input('Digite o primeiro dado: ')} {input('Digite o segundo dado: ')}"
print(f"Resultado da concatenação: {resultado}")